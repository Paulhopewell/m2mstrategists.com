# M2M Strategists site QA — 4 August 2026

## Confirmed and repaired on the review branch

- The Growth, AI & Exit Audit booking area no longer presents a fake form submission or claims that an email was sent.
- `Book Your Audit` now hands the visitor to the Phase 1 secure checkout at the verified staging route:
  `https://staging-m2m.paulhopewell.com/growth-ai-exit/checkout`
- The staging route was checked on 4 August 2026 and returned a redirect to `/checkout`; the checkout page returned HTTP 200 after following the redirect.
- Static preview pages and the core image/CSS assets returned HTTP 200 from the Netlify branch preview.
- `git diff --check` passed.

## Must be resolved before merging this branch to `main`

1. Replace the staging checkout hostname with the approved production app hostname. `app.m2mstrategists.com` is not live yet and must not be guessed or substituted until verified.
2. Confirm the public Audit price. The page currently shows `£995` in several places but also says `£1,495` with a `£500` Concierge credit/refund condition. This is a commercial decision, not a code repair.
3. Decide how the contact, newsletter and book-notification forms should be connected. They currently show success locally without sending anything.
4. Eight article links on `articles.html` point to files not present in this repository. Either restore the approved articles or remove/replace those cards before launch.
5. Replace remaining `href="#"` calls to action, including `Book a Call with Paul`, `Access Free Resources`, and `Start the Conversation`, with approved destinations.
6. Update internal repository documentation still headed `paulhopewell.com` and containing the old branch workflow instructions.
7. Add and verify `robots.txt` and `sitemap.xml` before launch.

## Scope boundary

No production branch, Netlify settings, Easyspace DNS or Google Workspace records were changed by this QA pass.
